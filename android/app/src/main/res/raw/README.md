Drop `ringtone_default.mp3` here to override the Android incoming-call ringtone.
The name (without extension) is what `AndroidParams.ringtonePath` refers to in
`lib/core/notification/callkit_service.dart`. Without a file the system default
is used.
